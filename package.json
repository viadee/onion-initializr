{
  "name": "@onion-initializr/root",
  "version": "0.0.18",
  "private": true,
  "description": "Onion Architecture Initializr - Monorepo root",
  "scripts": {
    "build": "nx run-many -t build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "format": "prettier --write .",
    "clean": "pnpm -r clean",
    "dev": "pnpm --filter gh-pages dev",
    "cli:dev": "pnpm --filter cli dev",
    "cli:build": "pnpm --filter cli build",
    "cli:link": "pnpm --filter cli link",
    "gh-pages:build": "pnpm --filter gh-pages github-build",
    "gh-pages:deploy": "pnpm --filter gh-pages github-deploy"
  },
  "keywords": [
    "onion-architecture",
    "architecture",
    "cli",
    "typescript",
    "monorepo"
  ],
  "author": "viadee",
  "license": "MIT",
  "devDependencies": {
    "@nx/eslint": "22.0.2",
    "@nx/vite": "22.0.2",
    "@nx/web": "22.0.2",
    "@vitest/ui": "^3.0.0",
    "eslint": "~8.57.0",
    "jiti": "2.4.2",
    "nx": "22.0.2",
    "prettier": "^3.0.0",
    "rimraf": "^5.0.0",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {}
}
